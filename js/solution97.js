/* Reverse List Order

In this kata you will create a function that takes in a list and returns a list with the reverse order.*/

// The parameter is an array
// We want to return the the array in reverse order
// We can use the array method of .reverse() to do so

const reverseList = list => list.reverse();


/*Test Cases   
reverseList([1,2,3,4]) => Output: [4,3,2,1]
reverseList([3,1,5,4]) => Output: [4,5,1,3]
*/